change_grade.py (string)Name (integer)NewGrade

Description
	This python script will search the standard input for the line containing Name, and replace the grade value 
	with NewGrade. The script assumes that the numerical grade is the last item on each line, and will 
	replace accordingly.


*Note
	Name is case sensitive

Example usage
	change_grade.py Bill 90 < ./grades.csv




